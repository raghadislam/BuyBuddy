<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Your account is verified — {{appName}}</title>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
      Arial; background: #f5f7fb; margin: 0; padding: 0; } .wrap { max-width:
      600px; margin: 28px auto; background: #fff; border-radius: 10px; overflow:
      hidden; box-shadow: 0 6px 18px rgba(2, 6, 23, 0.08); } .header { padding:
      20px; text-align: center; background: linear-gradient(90deg, #0ea5e9,
      #7c3aed); color: #fff; } .header .brand { font-weight: 700; font-size:
      18px; } .body { padding: 20px; color: #0b1220; line-height: 1.5; } h1 {
      font-size: 18px; margin: 0 0 12px; } .small { font-size: 13px; color:
      #6b7280; margin-top: 14px; } .footer { padding: 16px; font-size: 12px;
      color: #94a3b8; text-align: center; } @media (max-width: 420px) { .wrap {
      margin: 12px; } }
    </style>
  </head>
  <body>
    <div class="wrap">
      <div class="header">
        <div class="brand">{{appName}}</div>
      </div>

      <div class="body">
        <h1>You're all set — account verified ✅</h1>

        <p>Hi {{#if name}}{{name}}{{else}}there{{/if}},</p>

        <p>
          Your
          {{appName}}
          account has been successfully verified. You can now sign in and start
          using the app.
        </p>

        <p class="small">
          Time of verification:
          {{time}}
        </p>

        <p class="small">
          For your safety: if you didn't verify this account or don’t recognize
          this action, please contact support immediately at
          <a href="mailto:{{supportEmail}}">{{supportEmail}}</a>.
        </p>
      </div>

      <div class="footer">
        ©
        {{year}}
        {{appName}}
        •
        <a href="mailto:{{supportEmail}}">Contact support</a>
      </div>
    </div>
  </body>
</html>