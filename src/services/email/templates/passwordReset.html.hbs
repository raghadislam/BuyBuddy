<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Reset your password — {{appName}}</title>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
      Arial; background: #f5f7fb; margin: 0; padding: 0; } .wrap { max-width:
      600px; margin: 28px auto; background: #fff; border-radius: 10px; overflow:
      hidden; box-shadow: 0 6px 18px rgba(2, 6, 23, 0.08); } .header { padding:
      20px; text-align: center; background: linear-gradient(90deg, #0ea5e9,
      #7c3aed); color: #fff; } .header .brand { font-weight: 700; font-size:
      18px; } .body { padding: 20px; color: #0b1220; line-height: 1.5; } h1 {
      font-size: 18px; margin: 0 0 12px; } .muted { font-size: 13px; color:
      #6b7280; margin-top: 14px; } .code-box { display:block; margin:18px auto;
      padding:18px 14px; text-align:center; font-size:28px; letter-spacing:4px;
      font-weight:700; background:#f1f5f9; border-radius:8px; width:fit-content;
      color:#0b3b6f; } .btn { display:inline-block; padding:12px 18px;
      border-radius:8px; text-decoration:none; font-weight:600; margin-top:12px;
      background:#0b6cff; color:#fff; } .footer { padding: 16px; font-size:
      12px; color: #94a3b8; text-align: center; } @media (max-width: 420px) {
      .wrap { margin: 12px; } .code-box { font-size: 24px; letter-spacing:3px }
      }
    </style>
  </head>
  <body>
    <div class="wrap">
      <div class="header">
        <div class="brand">{{appName}}</div>
      </div>

      <div class="body">
        <h1>Password reset request</h1>

        <p>Hi {{#if name}}{{name}}{{else}}there{{/if}},</p>

        <p>
          We received a request to reset the password for your
          <strong>{{appName}}</strong>
          account. Use the code below to reset your password. The code will
          expire in
          <strong>{{ttlMinutes}}
            minute{{#unless ttlMinutes}}s{{/unless}}</strong>.
        </p>

        <!-- Code box -->
        <div
          class="code-box"
          role="textbox"
          aria-label="Password reset code"
        >{{code}}</div>

        <p class="muted">
          If you didn't request this, you can safely ignore this email — no
          changes were made to your account.
        </p>

        <p class="muted" style="margin-top:12px;">
          Time:
          {{time}}
        </p>

        <p class="muted" style="margin-top:8px;">
          For help, contact
          <a href="mailto:{{supportEmail}}">{{supportEmail}}</a>.
        </p>
      </div>

      <div class="footer">
        ©
        {{year}}
        {{appName}}
        •
        <a href="mailto:{{supportEmail}}">Contact support</a>
      </div>
    </div>
  </body>
</html>