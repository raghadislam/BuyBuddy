<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Password changed — {{appName}}</title>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
      Arial; background: #f5f7fb; margin: 0; padding: 0; } .wrap { max-width:
      600px; margin: 28px auto; background: #fff; border-radius: 10px; overflow:
      hidden; box-shadow: 0 6px 18px rgba(2, 6, 23, 0.08); } .header { padding:
      20px; text-align: center; background: linear-gradient(90deg, #0ea5e9,
      #7c3aed); color: #fff; } .header .brand { font-weight: 700; font-size:
      18px; } .body { padding: 20px; color: #0b1220; line-height: 1.5; } h1 {
      font-size: 18px; margin: 0 0 12px; } .muted { font-size: 13px; color:
      #6b7280; margin-top: 14px; } .alert { display:block; margin:14px 0;
      padding:12px; border-radius:8px; background:#f8fafc; color:#0b1220;
      font-weight:600; } .footer { padding: 16px; font-size: 12px; color:
      #94a3b8; text-align: center; } a { color: #0b6cff; text-decoration: none;
      } @media (max-width: 420px) { .wrap { margin: 12px; } }
    </style>
  </head>
  <body>
    <div class="wrap">
      <div class="header">
        <div class="brand">{{appName}}</div>
      </div>

      <div class="body">
        <h1>Your password was changed</h1>

        <p>Hi {{#if firstName}}{{firstName}}{{else}}there{{/if}},</p>

        <p class="muted">
          This is a confirmation that the password for your
          <strong>{{appName}}</strong>
          account was changed successfully.
        </p>

        <div class="alert">
          Changed on:
          {{time}}
        </div>

        <p class="muted">
          If you made this change, no further action is required.
        </p>

        <p class="muted">
          If you did
          <strong>not</strong>
          make this change or suspect unauthorized access, please reset your
          password immediately or contact support at
          <a href="mailto:{{supportEmail}}">{{supportEmail}}</a>.
        </p>
      </div>

      <div class="footer">
        ©
        {{year}}
        {{appName}}
        •
        <a href="mailto:{{supportEmail}}">Contact support</a>
      </div>
    </div>
  </body>
</html>